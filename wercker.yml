box: golang:1.5.1

build:
  steps:
    - arjen/hugo-build@1.8.4:
        theme: repl

deploy:
  steps:
    - uetchy/gh-pages@0.3.1:
        token: $GITHUB_TOKEN
        repo: jbowes/jbowes.github.io
        path: public
